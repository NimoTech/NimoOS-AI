[common]
RuntimePath = /var/run/nimoos
DataPath = /var/lib/nimoos/ai
LogPath = /var/log/nimoos

[ai]
MasterKeyPath = /etc/nimoos/ai_master.key

[agent]
AgentURL = http://127.0.0.1:8282
AgentTimeout = 60
OllamaURL = http://127.0.0.1:11434

[openvino]
URL = http://127.0.0.1:9100
Enabled = true
# 常驻设备(逗号分隔),第一个为省略 @device 时的默认。默认只常驻独显 GPU.1;
# 要同时常驻核显写 "GPU.1,GPU.0"(核显吃 32GB 共享内存,偏紧)。
Devices = GPU.1
# 同时最多驻留几个模型(对齐 Ollama OLLAMA_MAX_LOADED_MODELS);受显存约束,超出按 LRU 卸载。
MaxLoadedModels = 3
# 空闲多少分钟后自动卸载释放显存(对齐 Ollama keep_alive 默认 5 分钟);0 = 永不卸载。
IdleTTLMinutes = 5
# 每个模型(servable)的 KV cache 池大小,单位 GB(OVMS cache_size),决定有效上下文长度,
# 所有并发请求 + prefix cache 共享。默认 2(按 24GB B60 标定);显存更大的卡可调大,
# 注意最坏情况显存占用 ≈ 权重 + MaxLoadedModels × CacheSizeGB。改后需重启,且已加载的
# 模型要等下次重新加载(空闲卸载/重启 OVMS)才用新值。
CacheSizeGB = 2

# Attachment limits (override via env NIMOOS_MAX_ATTACHMENT_SIZE etc.)
MaxAttachmentSize         = 524288000     ; 500 MB, global upper bound
MaxImageAttachmentSize    = 20971520      ; 20 MB, image kind only
MaxAttachmentsPerSession  = 50
MaxAttachmentTextChars    = 32768         ; per read_attachment() call
FFProbeTimeout            = 5             ; seconds
AttachmentGCAge           = 86400         ; seconds, draft cleanup threshold
